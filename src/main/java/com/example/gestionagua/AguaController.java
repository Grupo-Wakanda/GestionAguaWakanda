package com.example.gestionagua;

import org.springframework.stereotype.Controller;

@Controller
public class AguaController {
    //tiene que getMapping desde graphql los datos publicados (fuga, calidad y riego) por el sensor
    //tiene que postMapping para enviar los datos al frontend

}
